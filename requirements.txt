streamlit==1.28.1
opencv-python-headless==4.8.1.78
Pillow==10.0.1
numpy==1.24.3
plotly==5.17.0
pandas==2.0.3
scikit-learn==1.3.0
matplotlib==3.7.2
# seaborn only if you use it (optional)
# seaborn==0.12.2
streamlit-drawable-canvas
streamlit-image-coordinates
# tensorflow removed — add back only if required and if you confirm the platform supports it
# tensorflow-cpu==2.13.0   # <-- enable only if you really need TF and tested locally
